# secrets
.env
.env.*

# python
__pycache__/
*.pyc
.venv/

# node / vite
node_modules/
ui/dist/
ui/.vite/

# EDA sim / synth artifacts (regenerable by the harness)
*.vcd
*.vvp
*.out
*_synth_*.txt

# AI-drafted, human-not-yet-reviewed chip specs -- never trusted, never committed
drafts/

# cached Devin Playbook id (env/org-specific, recreated automatically if missing)
orchestrator/.coder_playbook_id

# editor / OS
.DS_Store
